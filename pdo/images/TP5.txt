Task 1:
01- echo -e "test \n test"
    echo -e "test \t test"
02- history
03- echo $HISTFILE
04- cat $HISTFILE
05- env
    export PATH="$PATH:path_to_directory_to_add"
    USERNAME: This variable holds the name of the currently logged-in user
    HOME: This variable contains the path to the current user's home directory.
    PWD: This variable holds the absolute path of the current directory the user is in.
    LANG: This variable determines the default language and locale settings for the system.
    LC_TIME: This variable is part of the locale settings and specifically controls the formatting of time and date values.
    SHELL: This variable indicates the current user's default shell.
    HISTORY: This variable determines the maximum number of lines that the history file can contain.
    HISTSIZE: This variable defines the number of commands that will be stored in the history list
06- set
07- touch myscript.sh
    chmod +x myscript.sh
    ./myscript.sh
08- set +x
09- MY_VAR="my_value"
10- echo $MY_VAR
    my_value
11- bash
    echo $MY_VAR

    #no variable MY_VAR
12- exit
    echo $MY_VAR
    my_value
13- unset MY_VAR
    echo $MY_VAR

14- export MY_VAR="my_value"
    echo $MY_VAR
    my_value
15- bash
    echo $MY_VAR
    my_value
16- echo $MY_VAR

    #no variable MY_VAR in new bash session

Task 2:
01- find . -type f -name "myscript.sh"
02- find . -type f -name "*.sh"
03- sudo find /var -type f -name "*.log"
04- sudo find / -maxdepth 4 -type f -name "*.log"
05- touch mytestfile
    sudo apt install mlocate
    locate mytestfile
06- sudo updatedb
    locate mytestfile
07- -i, --ignore-case
08- whereis init
09- whereis -m init
10- whereis -s init
11- which chmod
12- which -a chmod
13- type ls cp pwd
    ls, cp are aliases, an alias is a shortcut or a custom name that represents a longer command or a command with specific options.
    pwd is a shell builtin, a builtin is a command that is built into the shell itself.

Task 3:
01- ls > lsfile
02- cat lsfile
03- nl <<< lsfile
04- ls /usr/bin >> lsfile
    cat lsfile
05- ls /usr/bin | nl
06- ls -e 2>errorfile
07- ls -l 1>lsfile 2>errorfile
08- ls > lsfile 2>&1
09- nl <<< "$MY_VAR"
10- ls | tee lsfile
11- ls | tee -a lsfile
12- echo -e "lsfile\nerrorfile" > xargsfile
13- cat xargsfile | xargs rm
